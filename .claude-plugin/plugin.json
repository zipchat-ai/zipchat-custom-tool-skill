{
  "name": "zipchat-custom-tool",
  "description": "Build a Zipchat custom tool — the prompt, variables, channels, and merchant note — that lets the AI support agent call an external HTTP API via curl. No codebase access required.",
  "version": "1.3.0",
  "author": { "name": "Ruslan Leteyski" }
}
